## analysis

First, read the requirement marked by C1 to C5 first, and tell me the idea to solve them. After my agreement, write down the code.

## test

When I ask you "test", run `airport_sched.mzn` for all the file in folder `data` and store the output in folder `sol` in format json.